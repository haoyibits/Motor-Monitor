# DAPLink/CMSIS-DAP over SWD, targeting the STM32F407 family.
source [find interface/cmsis-dap.cfg]
transport select swd

source [find target/stm32f4x.cfg]

# Lower this value if the connection is unstable on long jumper wires.
adapter speed 2000
