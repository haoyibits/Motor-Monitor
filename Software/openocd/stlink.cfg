# ST-Link over SWD, targeting the STM32F407 family.
# NRST is recommended but is not required for normal programming/debugging.
source [find interface/stlink.cfg]
transport select hla_swd

source [find target/stm32f4x.cfg]

# Lower this value if the connection is unstable on long jumper wires.
adapter speed 2000
